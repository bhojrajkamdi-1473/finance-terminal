# Cloudflare Pages build output (generated by build_static.py — never commit)
dist/
# Internal-only: audit registry + audit doc contain non-public verification notes
/data/
/docs/
# OS / editor junk
Thumbs.db
Desktop.ini
~$*
*.tmp
# Finance terminal runtime storage (sqlite) — never commit user data
/terminal-data/
# Local secrets / credentials — never commit (see .env.example for names only)
.env
*.db
# Python bytecode caches
__pycache__/
*.pyc
